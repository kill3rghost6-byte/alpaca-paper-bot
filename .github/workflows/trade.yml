name: Alpaca Live Trading Bot

# ⛔ COMPLETELY DISABLED — do not re-enable without explicit instruction
# Disabled on: 2026-07-01
on:
  workflow_dispatch:
    # Manual trigger only — cron and push triggers removed

jobs:
  run-bot:
    runs-on: ubuntu-latest
    if: false   # This line prevents the job from EVER running, even on manual dispatch

    steps:
      - name: DISABLED
        run: echo "This workflow is permanently disabled."
